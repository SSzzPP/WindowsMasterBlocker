# WindowsMasterBlocker

a nifty batch script that apply firewall rules to apps you don't want to access internet.

## How to run the script?

Run `WinMasterBlocker.bat` from the command line **as administrator**.
